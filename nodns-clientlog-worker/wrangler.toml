name = "nodns-clientlog"
main = "src/index.ts"
compatibility_date = "2025-06-01"

[observability]
enabled = false
head_sampling_rate = 0

# Custom domain: clientlog.nodns.shop
# Configured via Cloudflare dashboard or:
# npx wrangler custom-domain add clientlog.nodns.shop
